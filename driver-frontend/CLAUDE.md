@AGENTS.md

## Next.js Conventions

- The `middleware` file convention is deprecated in this project. Use `proxy.ts` with `export function proxy(request: NextRequest)` instead. Do NOT create or suggest `middleware.ts`.
